<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Economy - <%= guildId %></title>
    <link rel="stylesheet" href="/style.css">
    <style>
        .economy-container { max-width: 1200px; margin: 40px auto; padding: 20px; }
        .section { background: var(--card-bg); padding: 30px; border-radius: 10px; box-shadow: 0 2px 10px var(--shadow); border: 1px solid var(--border-color); margin-bottom: 30px; }
        .section h2 { color: var(--primary-color); margin-bottom: 20px; }
        table { width: 100%; border-collapse: collapse; }
        table th { background: var(--card-secondary); padding: 12px; text-align: left; color: var(--text-color); font-weight: 600; border-bottom: 2px solid var(--border-color); }
        table td { padding: 12px; border-bottom: 1px solid var(--border-color); color: var(--text-color); }
        table tr:hover { background: var(--card-secondary); }
    </style>
</head>
<body>
    <nav class="navbar">
        <div class="container">
            <div class="nav-brand">
                <h1>💰 Economy Dashboard</h1>
            </div>
            <div class="nav-links">
                <a href="/dashboard">Dashboard</a>
                <a href="/logout" class="btn-logout">Logout</a>
            </div>
        </div>
    </nav>

    <div class="container main-content">
        <!-- Quick Navigation -->
        <div class="quick-nav">
            <a href="/dashboard/<%= guildId %>" class="nav-btn">⚙️ Settings</a>
            <a href="/dashboard/<%= guildId %>/economy" class="nav-btn active">💰 Economy</a>
            <a href="/dashboard/<%= guildId %>/moderation" class="nav-btn">🛡️ Moderation</a>
            <a href="/dashboard/<%= guildId %>/automod" class="nav-btn">🤖 Auto-Mod</a>
            <a href="/dashboard/<%= guildId %>/shop" class="nav-btn">🛒 Shop</a>
            <a href="/premium" class="nav-btn">✨ Premium</a>
        </div>

    <div class="economy-container">

        <div class="section">
            <h2>💰 Economy Leaderboard</h2>
            <table>
                <thead>
                    <tr>
                        <th>Rank</th>
                        <th>User</th>
                        <th>Balance</th>
                        <th>Level</th>
                        <th>Total XP</th>
                    </tr>
                </thead>
                <tbody>
                    <% leaderboard.forEach((user, index) => { %>
                    <tr>
                        <td><strong>#<%= index + 1 %></strong></td>
                        <td><%= user.username %></td>
                        <td>💵 <%= user.balance || 0 %></td>
                        <td>Level <%= user.level || 0 %></td>
                        <td><%= user.xp || 0 %> XP</td>
                    </tr>
                    <% }); %>
                </tbody>
            </table>
        </div>

        <div class="section">
            <h2>📊 Economy Statistics</h2>
            <p>Total users with economy profile: <strong><%= leaderboard.length %></strong></p>
            <p>Highest balance: <strong>💵 <%= leaderboard[0]?.balance || 0 %></strong></p>
            <p>Average balance: <strong>💵 <%= Math.round(leaderboard.reduce((a, b) => a + (b.balance || 0), 0) / leaderboard.length) %></strong></p>
        </div>
    </div>
</body>
</html>
